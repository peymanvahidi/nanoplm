[project]
name = "prott5-s"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dvc>=3.59.1",
    "mlflow>=2.21.2",
    "pyyaml>=6.0.2",
    "torch>=2.6.0",
    "transformers>=4.50.2",
]
