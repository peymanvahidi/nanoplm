# Processing parameters for UniRef50 dataset

# % of data used for validation
val_ratio: 0.1

# Maximum number of sequences to process, null means process all sequences
max_seqs_number: 202

# Minimum sequence length to process
min_sequence_length: 20

# Maximum sequence length to process
max_sequence_length: 512

# Batch size for tokenization
batch_size: 32